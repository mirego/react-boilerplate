import styled from '@emotion/styled/macro';
import {ReactComponent as OriginalLogo} from './logo.svg';

const Logo = styled(OriginalLogo)`
  flex: 0 0 100px;
  width: 100px;
  height: 100px;
`;

export default Logo;
